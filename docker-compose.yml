version: '3.8'
services:
  argus:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - argus_data:/data
    environment:
      - SHODAN_API_KEY=${SHODAN_API_KEY:-}
      - AISSTREAM_KEY=${AISSTREAM_KEY:-}
    restart: unless-stopped
volumes:
  argus_data:
