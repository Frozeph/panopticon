version: '3.8'
services:
  argus:
    image: ghcr.io/frozeph/panopticon:latest
    ports:
      - "3000:3000"
    volumes:
      - /DATA/AppData/argus:/data
    environment:
      - SHODAN_API_KEY=${SHODAN_API_KEY:-}
      - AISSTREAM_KEY=${AISSTREAM_KEY:-}
    restart: unless-stopped
